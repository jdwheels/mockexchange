spring:
  data:
    rest:
      base-path: /api
      detection-strategy: annotated
      default-media-type: application/json
  flyway:
    locations: classpath:/com/trivialepic/mockoverflow/objects/db/migration/common
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: test
            client-secret: test
