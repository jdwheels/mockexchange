apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mockexchange-posts-api.fullname" . }}-test-connection"
  labels:
    {{- include "mockexchange-posts-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mockexchange-posts-api.fullname" . }}:{{ .Values.service.port }}/actuator/health']
  restartPolicy: Never
