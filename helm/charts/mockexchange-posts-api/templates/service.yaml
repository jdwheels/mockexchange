apiVersion: v1
kind: Service
metadata:
  name: {{ include "mockexchange-posts-api.fullname" . }}
  labels:
    {{- include "mockexchange-posts-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mockexchange-posts-api.selectorLabels" . | nindent 4 }}
