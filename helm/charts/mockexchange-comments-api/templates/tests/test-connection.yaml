apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mockexchange-common.fullname" . }}-test-connection"
  labels:
    {{- include "mockexchange-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mockexchange-common.fullname" . }}:{{ .Values.service.port }}/actuator/health']
  restartPolicy: Never
